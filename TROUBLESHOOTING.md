# Yomo 扩展程序故障排除指南

## 错误按钮问题解决方案

### 1. 立即检查步骤

1. **打开 Chrome 开发者工具**
   - 按 `F12` 或右键点击扩展程序图标 → "检查"
   - 查看 Console 标签页中的错误信息

2. **检查扩展程序状态**
   - 在浏览器地址栏输入：`chrome://extensions/`
   - 找到 Yomo 扩展程序
   - 查看是否有错误信息或警告

3. **重新加载扩展程序**
   - 在 `chrome://extensions/` 页面
   - 点击 Yomo 扩展程序的刷新按钮
   - 或者点击"重新加载"按钮

### 2. 常见问题及解决方案

#### 问题 1: API 连接失败
**症状**: 扩展程序无法连接到后端服务
**解决方案**:
- 检查网络连接
- 确认 API 服务器是否正常运行
- 检查防火墙设置

#### 问题 2: 权限问题
**症状**: 扩展程序无法访问某些功能
**解决方案**:
- 在 `chrome://extensions/` 中检查权限设置
- 确保所有必要权限都已授予
- 重新安装扩展程序

#### 问题 3: 存储问题
**症状**: 扩展程序无法保存或读取数据
**解决方案**:
- 清除浏览器缓存和 Cookie
- 检查存储空间是否充足
- 重新安装扩展程序

#### 问题 4: 版本兼容性问题
**症状**: 扩展程序在特定 Chrome 版本上出现问题
**解决方案**:
- 更新 Chrome 浏览器到最新版本
- 检查扩展程序是否支持当前 Chrome 版本

### 3. 调试步骤

#### 步骤 1: 使用调试脚本
1. 打开任意网页
2. 按 `F12` 打开开发者工具
3. 在 Console 中粘贴并运行 `debug-extension.js` 的内容
4. 查看输出信息

#### 步骤 2: 检查 Background Script
1. 在 `chrome://extensions/` 页面
2. 找到 Yomo 扩展程序
3. 点击"Service Worker"链接
4. 查看 Console 中的错误信息

#### 步骤 3: 检查 Content Script
1. 在任意网页上按 `F12`
2. 查看 Console 中是否有扩展程序相关的错误

### 4. 重新安装步骤

如果以上步骤都无法解决问题，请按以下步骤重新安装：

1. **卸载扩展程序**
   - 在 `chrome://extensions/` 页面
   - 点击 Yomo 扩展程序的"移除"按钮

2. **清理浏览器数据**
   - 清除浏览器缓存
   - 清除扩展程序相关的存储数据

3. **重新安装**
   - 重新加载未打包的扩展程序
   - 或者重新打包并安装

### 5. 联系支持

如果问题仍然存在，请提供以下信息：

- Chrome 浏览器版本
- 操作系统版本
- 错误截图
- Console 错误信息
- 扩展程序版本

### 6. 预防措施

- 定期更新 Chrome 浏览器
- 保持扩展程序为最新版本
- 定期清理浏览器缓存
- 避免同时安装过多扩展程序

---

**注意**: 如果错误按钮仍然出现，请尝试在安全模式下测试扩展程序，以排除其他扩展程序的干扰。 